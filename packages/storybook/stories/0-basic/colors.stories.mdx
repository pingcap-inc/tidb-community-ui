import { colors } from '@pingcap-inc/tidb-community-ui'
import { Meta, Story } from '@storybook/addon-docs'
import { Col, Row, Space, Typography } from 'antd'
import ColorBox from './ColorBox'
const sizes = { xxl: 8, xl: 8, lg: 8, md: 8, sm: 12, xs: 24 }
import './colors.stories.less'

# Color Palette

## Usage

```js
import { colors } from '@pingcap-inc/tidb-community-ui'
console.log(colors.B4)
```

## Showcase

<Meta title="basic/basic" />

<Story name="Color Palette">
  <Space direction='vertical' size={16}>
    <Row wrap gutter={[16, 16]}>
      <Col {...sizes}>
        <Space direction='vertical' size='large'>
          <Typography.Title level={3}>主题色 + 衍生色</Typography.Title>
          {Array(10).fill(0).map((_, i) => (
            <ColorBox key={`B${i}`} name={`B${i}`} color={colors[`B${i}`]} />
          ))}
        </Space>
      </Col>
      <Col {...sizes}>
        <Space direction='vertical' size='large'>
          <Typography.Title level={3}>中性色</Typography.Title>
          {Array(8).fill(0).map((_, i) => (
            <ColorBox key={`F${i}`} name={`F${i}`} color={colors[`F${i}`]} />
          ))}
          {Array(3).fill(0).reverse().map((_, i) => (
            <ColorBox key={`M${i + 1}`} name={`M${i + 1}`} color={colors[`M${i + 1}`]} />
          ))}
        </Space>
      </Col>
    </Row>
    <Row wrap gutter={[16, 16]}>
      <Col {...sizes}>
        <Space direction='vertical' size='large'>
          <Typography.Title level={3}>功能色</Typography.Title>
          {Array(5).fill(0).map((_, i) => (
            <ColorBox key={`T${i + 1}`} name={`T${i + 1}`} color={colors[`T${i + 1}`]} />
          ))}
        </Space>
      </Col>
      <Col offset={0} {...sizes}>
        <Space direction='vertical' size='large'>
          <Typography.Title level={3}>定制色</Typography.Title>
            {Array(5).fill(0).map((_, i) => (
              <ColorBox key={`D${i + 1}`} name={`D${i + 1}`} color={colors[`D${i + 1}`]} />
            ))}
        </Space>
      </Col>
    </Row>
  </Space>
</Story>
